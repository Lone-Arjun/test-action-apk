{
  "name": "builderengine-app",
  "version": "1.0.0",
  "description": "BuilderEngine WebView App",
  "main": "src/index.html",
  "scripts": {
    "build": "npx cap sync",
    "android": "npx cap run android",
    "ios": "npx cap run ios"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0",
    "@capacitor/android": "^6.0.0",
    "@capacitor/ios": "^6.0.0",
    "@capacitor/network": "^6.0.0"
  }
}
